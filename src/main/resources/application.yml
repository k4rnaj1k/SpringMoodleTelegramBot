spring:
  datasource:
    username: ${DB_USR}
    password: ${DB_PASS}
    url: ${DB_URL}
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
bot:
  token: ${BOT_TOKEN}
  username: ${BOT_USR}
web:
  currenturl: ${URL}
moodle:
  url: ${MOODLE_URL}
server:
  error:
    include-message: always